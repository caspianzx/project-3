<p>SHOW APPTS OF <%= @salon.email %></p>

<% if @appointments %>
	<% @appointments.each do |appt| %>
		<p>Name:<%= appt.name %></p>
		<p>Phone: <%= appt.phone %></p>
		<p>Email: <%= appt.email %></p>
		<p>Date: <%= appt.date %></p>
		<p>Timeslot: <%= appt.timeslot %></p>
		<p>Svc Name: <%= appt.service.name %></p>
		<p>Svc Price: <%= appt.service.price %></p>
		<%= link_to 'Edit Appt', edit_appointment_path(:salon_id => @salon.id, :appt_id => appt.id), :class => 'btn btn-link' %>
		<%= link_to 'Destroy Appt', destroy_appointment_path(:salon_id => @salon.id, :appt_id => appt.id),
        method: :delete,
        data: { confirm: 'Are you sure?' } %>
	<% end %>
<% else %>
	<p>No appts yet!</p>
<% end %>

<%= link_to 'New Appt', new_appointment_path, :class => 'btn btn-link' %>