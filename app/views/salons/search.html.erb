<p>SALONS THAT HAVE <%= @services.first.name %></p>

<div class="container">
  <div class="row">

  	<% @salons.each do |salon| %>


  	<div class="col-4">
  		<a href ="/salons/<%=salon.id%>/appointments" class="aTag">
  		<div class="card">
  			<div class ="cardImage">
  				<img src= "<%=salon.detail.logo_url%>" class="card-img-top cardImage" alt="...">
  			</div>
  			</a>
 					 <div class="card-body">
    					<p class="card-text"><%= salon.detail.name %></p>
  					</div>
			</div>

  	</div>

    <% end %>
  </div>
</div>